import { createSignal } from "solid-js";

export const [gold, setGold] = createSignal(1212312312321311231231231233123000);
export const [turn, setTurn] = createSignal(1);
export const [turnsTillPay, setTurnTillPlay] = createSignal(0);
export const [pay, setPay] = createSignal(5);
